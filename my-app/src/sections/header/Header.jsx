import React from 'react';
import {StyledHeader} from "./StyledHeader";

const Header = () => {
    return (
        <StyledHeader>
            <nav>
                <a href="#">Text</a>
            </nav>
        </StyledHeader>
    );
};

export default Header;